import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
           Text("Nicholas")
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundColor(.accentColor)
            Text("James")
            Text("Hello")
        Text("James is not an idiot")
            
        }
    }
}
