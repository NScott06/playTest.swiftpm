import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
          Image("hwxtm6n79ma61")
                .resizable()
                .scaledToFit()
            
        }
    }
}
